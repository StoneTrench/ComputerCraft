local function DIORITE_FUNC()
    return {
        elements = {
            createButton = function (x, y, w, h, text, callback)
                
            end
        }
    }
end

DIORITE = DIORITE_FUNC();